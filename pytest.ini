[pytest]
testpaths = testcases
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    smoke: 冒烟测试
    positive: 正向场景
    negative: 异常场景
    structure: 结构校验

addopts = -v --tb=short --strict-markers
